/* pages/review/review.wxss */
@import "../../styles/tokens.wxss";

.review-page {
  min-height: 100vh;
  box-sizing: border-box;
  padding: 0 var(--qp-space-5) calc(100rpx + env(safe-area-inset-bottom));
  background-color: var(--qp-color-canvas);
  color: var(--qp-color-ink);
}

.review-head {
  margin-bottom: 20rpx;
}

.review-head__kicker {
  display: block;
  margin-bottom: 8rpx;
  font-family: var(--qp-font-family-data);
  font-size: 20rpx;
  font-weight: 700;
  color: var(--qp-color-editorial);
  letter-spacing: 0;
}

.review-head__title {
  display: block;
  font-size: var(--qp-font-size-masthead);
  font-weight: 800;
  color: var(--qp-color-ink);
  letter-spacing: 0;
  margin-bottom: 8rpx;
  line-height: 1;
}

.review-head__sub {
  font-size: var(--qp-font-size-body);
  color: var(--qp-color-text-secondary);
}

.review-rule {
  display: flex;
  align-items: center;
  margin-bottom: 28rpx;
}

.review-rule view {
  width: 60rpx;
  height: 4rpx;
  background: var(--qp-color-primary);
}

.review-rule text {
  flex: 1;
  height: 2rpx;
  background: var(--qp-color-ink);
  opacity: 0.72;
}

.review-section {
  margin-bottom: 24rpx;
}

.review-section__title {
  display: block;
  font-size: var(--qp-font-size-label);
  font-weight: var(--qp-font-weight-regular);
  letter-spacing: 2rpx;
  color: var(--qp-color-text-tertiary);
  margin-bottom: 12rpx;
}

.review-card {
  display: flex;
  flex-direction: column;
  min-height: 88rpx;
  padding: 24rpx;
  background-color: var(--qp-color-surface);
  border: var(--qp-border-width) solid var(--qp-color-line);
  border-radius: var(--qp-radius-lg);
  margin-bottom: 12rpx;
  position: relative;
}

.review-card__title {
  font-size: 28rpx;
  font-weight: var(--qp-font-weight-semibold);
  color: var(--qp-color-ink);
}

.review-card__sub {
  margin-top: 4rpx;
  font-size: 22rpx;
  color: var(--qp-color-text-secondary);
}

.review-card__chev {
  position: absolute;
  right: 24rpx;
  top: 50%;
  transform: translateY(-50%);
  font-size: 28rpx;
  color: var(--qp-color-text-tertiary);
  opacity: 0.45;
}
